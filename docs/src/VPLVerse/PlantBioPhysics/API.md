
# PlantBiophysics

```@meta
CurrentModule = PlantBiophysics
```

## Index

```@index
Modules = [PlantBiophysics]
Public = true
Private = true
```

## Public API

```@autodocs
Modules = [PlantBiophysics]
Public = true
Private = false
```

## Private

Private functions, types or constants from `PlantBiophysics`. These are not exported, so you need to prefix the function name with `PlantBiophysics.` to access them. Also bear in mind that these are not part of the public API, so they may change without notice.

```@autodocs
Modules = [PlantBiophysics]
Public = false
Private = true
```
